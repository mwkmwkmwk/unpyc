def f():
	while $global[a]:
		$global[b]
		return $global[c]
	else:
		$global[d]
	$global[e]
	while $global[a]:
		$global[b]
		if $global[x]:
			continue
		return $global[c]
	else:
		$global[d]
	$global[e]
	while $true:
		$global[b]
		return $global[c]
	else:
		$global[d]
	$global[e]
	while $true:
		$global[b]
		if $global[x]:
			continue
		return $global[c]
	else:
		$global[d]
	$global[e]
	while $true:
		$global[b]
		return $global[c]
		$global[d]
		return $global[e]
		$global[f]
		return $global[g]
	else:
		$global[h]
